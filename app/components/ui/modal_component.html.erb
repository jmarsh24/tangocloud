<%= content_tag :dialog, class: classes, **attributes do %>
  <div class="modal-box bg-black" data-modal-target="modalBox">
    <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2" data-action="click->modal#close">
      X
    </button>
    <%= content %>
  </div>
<% end %>
