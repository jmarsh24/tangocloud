<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <link rel="manifest" href="/manifest.json">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <meta name="theme-color" content="#000000">
  <meta name="turbo-cache-control" content="no-cache">
  <%= favicon_link_tag "logos/favicon.svg", type: "image/svg+xml" %>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= vite_client_tag %>
  <%= vite_typescript_tag "application", media: "all", "data-turbo-track": "reload" %>
  <%= vite_stylesheet_tag "application.css", media: "all", "data-turbo-track": "reload" %>
  <%= yield :additional_head_tags if content_for? :additional_head_tags %>
  <%= turbo_refreshes_with method: :morph, scroll: :preserve %>
  <%= yield :head %>
</head>
