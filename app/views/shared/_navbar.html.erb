<nav class="navbar bg-neutral-200 dark:bg-neutral-900 p-4 flex justify-between items-center relative" data-controller="hidden">
  <%= nav_link_to "TangoCloud", root_path, class: "text-xl font-extrabold text-gray-800 dark:text-gray-200 hover:text-gray-400 dark:hover:text-gray-500" %>
  <ul class="menu flex space-x-4 items-center">
    <% if user_signed_in? %>
      <li class="relative">
        <button class="flex items-center space-x-2 focus:outline-none" data-action="click->hidden#toggle" data-hidden-target="button">
          <% if current_user.avatar.present? %>
            <%= image_tag(current_user.avatar, alt: "User Avatar", class: "w-8 h-8 rounded-full") %>
          <% else %>
            <%= image_tag("icons/new-user.svg", alt: "User Avatar", class: "w-8 h-8") %>
          <% end %>
        </button>
        <div data-hidden-target="toggleable" class="dropdown-menu hidden absolute right-0 mt-2 w-48 bg-white dark:bg-neutral-800 rounded-lg shadow-lg z-50 overflow-hidden">
          <% if current_user&.admin? %>
            <%= link_to avo.root_path, class: "block px-4 py-2 text-gray-800 dark:text-gray-200 hover:bg-neutral-100 dark:hover:bg-neutral-700 rounded-t-lg" do %>
              Admin
            <% end %>
            <%= link_to mission_control_jobs_path, class: "block px-4 py-2 text-gray-800 dark:text-gray-200 hover:bg-neutral-100 dark:hover:bg-neutral-700" do %>
              Jobs
            <% end %>
          <% end %>
          <%= link_to edit_user_registration_path, class: "block px-4 py-2 text-gray-800 dark:text-gray-200 hover:bg-neutral-100 dark:hover:bg-neutral-700" do %>
            Edit Profile
          <% end %>
          <%= link_to destroy_user_session_path, 'data-turbo-method': :delete, class: "block px-4 py-2 text-red-400 hover:bg-red-100 dark:text-red-300 dark:hover:bg-red-100 rounded-b-lg" do %>
            Sign Out
          <% end %>
        </div>
      </li>
    <% else %>
      <li>
        <%= nav_link_to "Log in", new_user_session_path, class: "text-gray-800 dark:text-gray-200 hover:text-gray-400 dark:hover:text-gray-500" %>
      </li>
    <% end %>
  </ul>
</nav>
