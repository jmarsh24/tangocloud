<%= form_with url: load_orchestra_recording_path(orchestra_id: @recording.orchestra, id: @recording.id), class: "hidden" do |form| %>
  <%= button_tag type: :submit, data: { controller: "connect-submit" } %>
<% end %>
