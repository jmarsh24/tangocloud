<div id="search-results" class="container mx-auto p-4 md:p-8">
  <!-- Top Result Section -->
  <div class="flex flex-col md:flex-row gap-4">
    <% if @top_result.present? %>
      <div>
        <h2 class="text-xl md:text-2xl font-semibold mb-4">Top Result</h2>
        <div class="bg-neutral-800 rounded-lg flex flex-col md:flex-row p-4 md:p-6 gap-4 items-center">
          <%= render @top_result %> <!-- Assume this includes image and info -->
        </div>
      </div>
    <% end %>
    <!-- Recordings Section -->
    <% if @recordings.present? %>
      <div>
        <h2 class="text-xl md:text-2xl font-semibold mb-4">Recordings</h2>
        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-4 md:gap-6">
          <% @recordings.each do |recording| %>
            <div class="bg-neutral-800 rounded-lg p-4">
              <%= render recording %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
  <!-- Orchestras Section (Horizontal Scroll) -->
  <% if @orchestras.present? %>
    <div>
      <h2 class="text-xl md:text-2xl font-semibold mb-4">Orchestras</h2>
      <div class="flex space-x-4 overflow-x-auto scrollbar-hide">
        <% @orchestras.each do |orchestra| %>
          <div class="bg-neutral-800 rounded-lg shrink-0 w-40 sm:w-48 p-4">
            <%= render orchestra %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
  <!-- Playlists Section (Horizontal Scroll) -->
  <% if @playlists.present? %>
    <div>
      <h2 class="text-xl md:text-2xl font-semibold mb-4">Playlists</h2>
      <div class="flex space-x-4 overflow-x-auto scrollbar-hide">
        <% @playlists.each do |playlist| %>
          <div class="bg-neutral-800 rounded-lg shrink-0 w-40 sm:w-48 p-4">
            <%= render playlist %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
  <!-- Tandas Section (Horizontal Scroll) -->
  <% if @tandas.present? %>
    <div>
      <h2 class="text-xl md:text-2xl font-semibold mb-4">Tandas</h2>
      <div class="flex space-x-4 overflow-x-auto scrollbar-hide">
        <% @tandas.each do |tanda| %>
          <div class="bg-neutral-800 rounded-lg shrink-0 w-40 sm:w-48 p-4">
            <%= render tanda %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
