<%= form_with model: @tanda, local: true do |form| %>
  <div class="flex flex-col gap-4">
    <%= form.label :title, "Tanda Title" %>
    <%= form.text_field :title, class: "input" %>
    <%= form.submit "Create Tanda", class: "btn-primary" %>
  </div>
<% end %>