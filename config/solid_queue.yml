default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 500
      recurring_tasks:
  workers:
    - queues: "*"
      threads: 8
      processes: 1
      polling_interval: 0.1

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
